import { Callout } from 'nextra/components';

# @auth/mikro-orm-adapter

<div style={{display: "flex", justifyContent: "space-between", alignItems: "center", padding: 16}}>
 <p>Official <a href="https://mikro-orm.io/docs/installation">MikroORM</a> adapter for Auth.js / NextAuth.js.</p>
 <a href="https://mikro-orm.io/">
  <img style={{display: "block"}} src="https://authjs.dev/img/adapters/mikro-orm.svg" width="64"/>
 </a>
</div>

## Installation

```bash npm2yarn
npm install @mikro-orm/core @auth/mikro-orm-adapter
```

## defaultEntities

Renames and re-exports [lib/entities](mikro-orm-adapter/lib/entities.mdx)

## MikroOrmAdapter()

```ts
MikroOrmAdapter<D>(ormOptions, options?): Adapter
```

### Type parameters

| Type parameter | Value |
| :------ | :------ |
| `D` extends `IDatabaseDriver`\<`Connection`\> | `IDatabaseDriver`\<`Connection`\> |

### Parameters

| Parameter | Type |
| :------ | :------ |
| `ormOptions` | `Options`\<`D`\> |
| `options`? | `Object` |
| `options.entities`? | `Partial`\<[`lib/entities`](mikro-orm-adapter/lib/entities.mdx)\> |

### Returns

[`Adapter`](express/adapters.mdx#adapter)
