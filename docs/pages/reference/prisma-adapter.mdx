import { Callout } from 'nextra/components';

# @auth/prisma-adapter

<div style={{display: "flex", justifyContent: "space-between", alignItems: "center", padding: 16}}>
 Official <a href="https://www.prisma.io/docs">Prisma</a> adapter for Auth.js / NextAuth.js.
 <a href="https://www.prisma.io/">
  <img style={{display: "block"}} src="https://authjs.dev/img/adapters/prisma.svg" width="38" />
 </a>
</div>

## Installation

```bash npm2yarn
npm install @prisma/client @auth/prisma-adapter
npm install prisma --save-dev
```

## PrismaAdapter()

```ts
PrismaAdapter(prisma): Adapter
```

### Parameters

| Parameter | Type |
| :------ | :------ |
| `prisma` | `PrismaClient`\<`PrismaClientOptions`, `never`, `DefaultArgs`\> \| `DynamicClientExtensionThis`\<`TypeMap`\<`InternalArgs` & `InternalArgs`\<\{}, \{}, \{}, \{}\>\>, `TypeMapCb`, `InternalArgs`\<\{}, \{}, \{}, \{}\>\> |

### Returns

[`Adapter`](express/adapters.mdx#adapter)
